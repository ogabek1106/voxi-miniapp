// frontend/js/admin_reading/question_types/gap/render.js

window.AdminReading = window.AdminReading || {};
AdminReading.Gap = AdminReading.Gap || {};

/**
 * Render student inputs for gap questions
 * (stub for now)
 */
AdminReading.Gap.render = function(container, question) {

  if (!container) return;

  container.innerHTML = `
    <div style="padding:10px; border:1px dashed #ccc;">
      GAP RENDER PLACEHOLDER
    </div>
  `;

};
